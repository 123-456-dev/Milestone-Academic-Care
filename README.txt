# Firebase Full Project (Frontend + Backend + Firestore)

## Setup Instructions

1. Install Firebase CLI:
   npm install -g firebase-tools

2. Login:
   firebase login

3. Go to project folder:
   cd firebase_project

4. Initialize Firebase:
   firebase init (choose Hosting and Functions)

5. Replace Firebase config in index.html

6. Deploy:
   firebase deploy
